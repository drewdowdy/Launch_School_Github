// Is JavaScript a garbage-collected language, and if so, what does this entail?

// Answer: Yes. JS is a garbage-collected language; meaning that GC happens at runtime, rather than by the developer manually.
