// Edit the code from problem one so it executes without error.

(function() {
  console.log("Sometimes, syntax isn't intuitive!")
})();

// We can wrap the declaration in parenthesis to make it a function expression.
